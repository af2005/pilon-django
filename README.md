# Prerequisites 

- install poetry (e.g. ```brew install poetry```)
- ```poetry install```
- install npm (e.g. ```brew install npm```)
- ```npm install```

```npm run dev```

# Starting development server
``` make migrate```

``` make run ```